# ============================================================================
# FreeFlow Web (Next.js) - Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control!
#
# Public variables (NEXT_PUBLIC_*) are exposed to the browser
# Server variables (no prefix) are only available on the server
# ============================================================================

# ----------------------------------------------------------------------------
# Public Variables (Exposed to Browser) - NEXT_PUBLIC_*
# ----------------------------------------------------------------------------
# These are embedded at build time and visible in the browser bundle
# Safe to set to actual values (no secrets!)

# API Gateway URL
# Dev: http://localhost:4000
# Staging: https://api.staging.freeflow.dev
# Production: https://api.freeflow.dev
NEXT_PUBLIC_API_URL=http://localhost:4000

# Application name
NEXT_PUBLIC_APP_NAME=FreeFlow

# Application version
NEXT_PUBLIC_APP_VERSION=1.0.0

# Current environment
# Options: development | staging | production
NEXT_PUBLIC_ENVIRONMENT=development

# Enable analytics (optional)
# Set to true in production
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Sentry DSN for error tracking (optional)
# Get from: https://sentry.io/settings/projects/
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# ----------------------------------------------------------------------------
# Server Variables (Server-Side Only) - NO PREFIX
# ----------------------------------------------------------------------------
# These are NEVER exposed to the browser
# Only available in API routes, getServerSideProps, middleware, etc.

# Environment
NODE_ENV=development

# Session secret (REQUIRED - Generate new for each environment!)
# Generate with: openssl rand -hex 32
# MUST be at least 32 characters
SESSION_SECRET=your-session-secret-min-32-chars-change-me-in-production

# API key for server-to-server communication (optional)
# Generate with: openssl rand -hex 32
# API_KEY=your-api-key-for-internal-requests

# Logging level
# Options: error | warn | info | debug
LOG_LEVEL=info

# Analytics write key (server-side only, optional)
# ANALYTICS_WRITE_KEY=your-analytics-write-key

# ----------------------------------------------------------------------------
# Notes
# ----------------------------------------------------------------------------
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in all required values (marked REQUIRED)
# 3. Generate secure secrets for SESSION_SECRET
# 4. NEVER commit .env.local to git
# 5. Use different secrets for each environment
# 6. Rebuild required after changing NEXT_PUBLIC_* variables
