# P3.2 Validation

Date: 2026-02-15

## Local Provisioning Check (Not Run)

Planned steps:

1. `docker compose up -d` for postgres/mongodb/qdrant/keycloak.
2. Run `pnpm ts-node scripts/tenant-provision.ts acme`.
3. Verify:
   - Postgres DB `freeflow_acme` exists
   - Mongo DB `freeflow_acme` exists
   - Qdrant collection `freeflow_acme_vectors` exists
   - `storage/tenants/acme` folder exists

Result: **NOT RUN** (local infrastructure not started in this session).
