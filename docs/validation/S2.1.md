# Validation S2.1 — Context Server

## Start context-server

```bash
pnpm --filter @freeflow/context-server dev
```

## Verify contexts

```bash
curl -s http://localhost:8090/context/freeflow-energy.jsonld | head -n 5
curl -s http://localhost:8090/context/index.json
```
