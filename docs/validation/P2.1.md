# P2.1 Validation

Date: 2026-02-15

## Lint

Command:

```bash
pnpm --filter @freeflow/web lint
```

Result: **FAILED**

Notes:

- Existing repo warnings remain (prettier/import-order) across multiple files.
- `apps/web/src/lib/rbac.ts` fails with `import/no-unresolved` for `server-only`.

## Build

Command:

```bash
pnpm --filter @freeflow/web build
```

Result: **FAILED**

Notes:

- Build runs lint/type checks and fails with the same lint issues listed above.
